# Form validation async React
##### Form with country and cities select fields. Implemented using React, Formik, json-server, bootstrap4.

## Setup
```
npm install
npm start
```
than open  http://localhost:3000/
